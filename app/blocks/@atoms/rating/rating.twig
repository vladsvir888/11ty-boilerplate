{% if rating.href %}
  {% include "../../@atoms/button/button.twig" with {
    button: rating|default({})|merge({
      class: "rating " ~ rating.class
    })
  } %}
{% else %}
  <div class="rating {{ rating.class }}">
    {{ rating.text.text }}
  </div>
{% endif %}
